{
  "name": "rmc",
  "private": true,
  "description": "RMC 分析情報ページのデータ仕様・精算/集計ロジック・自動テスト（外部依存なし）",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "validate": "node scripts/validate-data.js",
    "check-locked": "node scripts/check-locked.js",
    "baseline": "node baseline/capture-baseline.js"
  }
}
